# API de Gemini (Requerido)
GEMINI_API_KEY=tu_clave_api_aquí
GEMINI_MODEL=gemini-2.0-flash

# Base de Datos H2 (Opcional)
DB_PATH=/data/geminidb
DB_USERNAME=sa
DB_PASSWORD=password

# Configuración del Servidor (Opcional)
PORT=8080

# Notas:
# - GEMINI_API_KEY es obligatorio para que la aplicación funcione
# - Los demás valores tienen valores por defecto y son opcionales
# - DB_PATH: Ruta donde se almacenará la base de datos H2
# - DB_USERNAME y DB_PASSWORD: Credenciales para la base de datos
# - PORT: Puerto en el que se ejecutará la aplicación